<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenusAndPickers"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppBarActivity -->
        <activity
            android:name=".AppBarActivity"
            android:exported="true" />

        <!-- ContextualMenuActivity -->
        <activity
            android:name=".ContextualMenuActivity"
            android:exported="true" />

        <!-- PopupMenuActivity -->
        <activity
            android:name=".PopupMenuActivity"
            android:exported="true" />

        <!-- DialogsActivity -->
        <activity
            android:name=".DialogsActivity"
            android:exported="true" />

        <!-- PickersActivity -->
        <activity
            android:name=".PickersActivity"
            android:exported="true" />

    </application>

</manifest>
